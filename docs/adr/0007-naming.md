# 0007 — Repo and package names

Repo `frederick-wang/pi-openrouter-balance`; npm `pi-openrouter-balance` (unscoped, verified free at creation; scoped fallback only if ever taken); command `/openrouter-balance`; env prefix `PI_OPENROUTER_BALANCE_*`; status key `pi-openrouter-balance`.
