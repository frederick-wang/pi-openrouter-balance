/**
 * pi-openrouter-balance
 *
 * Implementation lands per the ticket plan (see CONTEXT.md, docs/adr/, and
 * the repo issues). Kept as a minimal installable stub until ticket 02+.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function openRouterBalance(pi: ExtensionAPI): void {
	void pi;
}
